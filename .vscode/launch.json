{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch OpenCL Program",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/${fileDirname}/${file}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "build",
            "miDebuggerPath": "/usr/bin/lldb",
            "internalConsoleOptions": "openOnSessionStart"
        }
    ]
}
